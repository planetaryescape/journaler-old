{
  "name": "journaler",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "env": "doppler setup -c dev -p journaler-webapp",
    "dev": "doppler run -- next dev | pino-pretty",
    "build": "next build",
    "start": "next start",
    "db:generate": "doppler run -- drizzle-kit generate",
    "db:studio": "doppler run -- drizzle-kit studio",
    "db:migrate": "doppler run -- drizzle-kit migrate",
    "lint": "next lint"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.10",
    "next": "14.2.3",
    "postgres": "^3.4.4",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "drizzle-kit": "^0.21.1",
    "eslint": "^8",
    "eslint-config-next": "14.2.3",
    "pino-pretty": "^11.0.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}